import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

//ReactDOM -this means print it
ReactDOM.render(                
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
